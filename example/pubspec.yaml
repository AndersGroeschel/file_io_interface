name: example
description: Example app for Abstract IO using the file_io_interface package to Implement an IOInterface

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  abstract_io: ^0.1.0
  file_io_interface: ^0.1.0



flutter:
  uses-material-design: true